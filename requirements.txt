# ---- Core dependencies ----
# Egocentric data access (Project Aria / Aria Digital Twin dataset)
# Pinned to the version the thesis was developed/tested against; newer Meta
# releases may change the ADT API (e.g. AriaDigitalTwinDataProvider, gaze reprojection).
projectaria-tools==1.5.8

# Numerics & data handling
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3

# LLM prediction layer
openai>=1.0          # OpenAI Python SDK (GPT-4o / GPT-4o-mini)
tiktoken>=0.5        # token counting before each request
pyyaml>=6.0          # parsing the structured LLM response

# Visualization & utilities
rerun-sdk>=0.16      # 3D visualization (rerun.io)
tqdm>=4.65           # progress bars
openpyxl>=3.1        # debug logging to Excel

# ---- Optional: alternative Llama backend ----
# Only needed if you use utils/llama.py instead of the OpenAI backend.
# llamaapi>=0.1
# torch>=2.0
# transformers>=4.40
